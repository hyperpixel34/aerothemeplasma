./build-decoration.sh
./install-decoration.sh
./build-effect.sh
./install-effect.sh

restart kwin with the command

$ kwin_x11 --replace &

you can use rebuild-decoration.sh and rebuild-effect.sh to quickly rebuild the programs without starting all over again.
